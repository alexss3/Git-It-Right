import styled from 'styled-components';

import ListGroup from 'react-bootstrap/ListGroup';

export const IssuesContainer = styled(ListGroup)`
    span.counts {
        display: inline-block;
        margin-right: 0.5em;
    }
`;